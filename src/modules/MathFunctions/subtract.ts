/**
 * Subtracts two numbers
 * @param a First number
 * @param b Second number to subtract from first
 * @returns The difference of a and b
 */

export function subtract(a: number, b: number): number {
    return a - b;
}
