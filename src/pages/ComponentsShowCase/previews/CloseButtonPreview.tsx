import { CloseButton } from 'kalki-ui';
export const CloseButtonPreviews = {
  basic: (
    <div className="flex gap-2 items-center">
      <CloseButton variant="light" size="sm" />
      <CloseButton variant="light" size="md" />
      <CloseButton variant="dark" size="sm" />
    </div>
  ),
};
