import { SectionHeader } from '@/components/common/SectionHeader';

export const SectionHeaderPreviews = {
  basic: (
    <SectionHeader variant="default" size="md" align="left">
      <SectionHeader.Title>Section Title</SectionHeader.Title>
      <SectionHeader.SubTitle>Optional subtitle or description.</SectionHeader.SubTitle>
    </SectionHeader>
  ),
};
